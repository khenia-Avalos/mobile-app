import { useAuth } from '../contexts/authContext';
export { useAuth };

//ES EL MISMO USEAUTH 
// Hook para auth	
// TypeScript añade tipos para mejor autocompletado	
// Exporta el mismo hook pero con tipos TypeScript